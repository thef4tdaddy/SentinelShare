# App Configuration
POLL_INTERVAL=60
SECRET_KEY=change_this_to_something_secret
DASHBOARD_PASSWORD=your_secure_password

# Multi-User Configuration
# Set to false to disable public registration (only admins can create users)
ALLOW_REGISTRATION=true
# Optional: Set the frontend URL for OAuth2 redirects
# FRONTEND_URL=https://your-domain.com

# Email Credentials (IMAP/SMTP) - Legacy/Manual Setup
# Note: In multi-user mode, it's recommended to add email accounts via the web UI
# These environment variables are kept for backward compatibility
GMAIL_EMAIL=your.email@gmail.com
GMAIL_PASSWORD=your_app_password
WIFE_EMAIL=wife.email@example.com
SENDER_EMAIL=your.email@gmail.com
SENDER_PASSWORD=your_app_password

# OAuth2 Configuration (Recommended for Gmail)
# Get credentials from Google Cloud Console: https://console.cloud.google.com/
# Enable Gmail API and create OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# OAuth2 Configuration for Microsoft (Coming Soon)
MICROSOFT_CLIENT_ID=your_microsoft_client_id
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret

# Database Configuration
# For Docker with PostgreSQL (docker-compose.yml), set this:
POSTGRES_PASSWORD=changeme_secure_postgres_password

# For Docker with SQLite (docker-compose.sqlite.yml), no database configuration needed
# For Heroku or custom deployments, set DATABASE_URL:
# DATABASE_URL=postgresql://user:pass@localhost:5432/receipts
